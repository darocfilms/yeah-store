# Carpeta de descargas — PLACEHOLDER

Estos archivos son marcadores de posición. Reemplázalos por los `.zip` reales de cada producto (mismo nombre que `downloadFile` en `/products.json`) antes de lanzar la tienda:

- `halation-dctl.zip`
- `film-grain-engine.zip`
- `print-film-pack.zip`
- `gate-weave-flicker.zip`

Mientras tanto, el correo de entrega enlaza a estos archivos placeholder para que puedas probar el flujo completo de compra sin que el enlace rompa (404).

Nota de seguridad: cualquier archivo en esta carpeta queda descargable públicamente por cualquiera que adivine la URL, sin verificar que haya pagado. Para una tienda real conviene generar enlaces firmados/temporales (por ejemplo con Netlify Blobs + un token de un solo uso) en vez de servir los ZIP como archivos estáticos. Ver `DEPLOY.md`.
